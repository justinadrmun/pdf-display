# pdf-display

A simple project for displaying and benchmarking PDF display strategies.

## Setup

Use [just](https://github.com/casey/just) to manage common tasks:

- **Initialize environment and install dependencies:**
  
  ```sh
  just init
  ```

- **Run the benchmark app:**
  
  ```sh
  just benchmark
  ```